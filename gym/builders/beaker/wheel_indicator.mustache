  <!-- {{dir}} Wheel Indicator .................................................................-->
  <link name="{{dir}}WheelIndicator">
    <visual>
      <geometry>
        <box size="{{indicator_dimensions}}"/>
      </geometry>
      <material name="orange"/>
    </visual>
    <inertial>
        <mass value=".0000" />
        <inertia ixx="0.000000" ixy="0.0" ixz="0.0" iyy="0.000000" iyz="0.0" izz="0.000000"/>
    </inertial>
  </link>
  <joint name="indicatorTo{{dir}}Wheel" type="fixed">
    <origin xyz="0 {{wheel_rad}} 0"/>
    <parent link="{{dir}}Wheel"/>
    <child link="{{dir}}WheelIndicator"/>
  </joint>